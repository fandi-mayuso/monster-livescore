class AppConstants {
  // API configuration (same for all environments)
  /// HTTP request timeout in seconds
  static const String apiTimeout = '30';
  
  // Feature flags (can be enabled/disabled globally)
  /// Whether to collect analytics data
  static const bool enableAnalytics = true;
  
  // App information
  /// Current app version
  static const String appVersion = '1.0.0';
}
